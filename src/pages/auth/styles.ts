import { styled } from "../../../stitches.config";

export const AuthContainer = styled('div', {
  width: '100vw',
  height: '100vh',
  padding: '0 1.25rem',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center'
})